# CLAUDE.md

Instructions for Claude Code when working in this repository.

Read [AGENTS.md](AGENTS.md) for project overview, directory structure, behavioral guidelines, editing rules, and common commands.

All content in AGENTS.md applies to Claude Code as well.

## Claude Code Notes

- Ruby/Bundler path may need to be set: `export PATH="/usr/local/opt/ruby/bin:$PATH"`
- Use `bundle exec jekyll` (not bare `jekyll`) to ensure correct gem versions.
